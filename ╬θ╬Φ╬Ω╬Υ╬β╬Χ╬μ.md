# CoolFix: μίνι εφαρμογή διαχειριστή

## Τι περιέχει το πακέτο

```
src/index.js                ← ο Worker: σερβίρει τη σελίδα + το κλειδωμένο API
public/admin/index.html     ← η εφαρμογή (login, νέο ραντεβού, λίστα, γραφήματα)
schema.sql                  ← οι πίνακες της βάσης
seed-demo.sql               ← προαιρετικά: δοκιμαστικά ραντεβού για το demo
wrangler.jsonc              ← ρυθμίσεις (assets + βάση)
public/index.html           ← η σελίδα σου, με το κουμπάκι-λουκέτο ήδη μέσα
public/style.css            ← το CSS σου + το στυλ του κουμπιού (στο τέλος)
public/script.js            ← αμετάβλητο
.dev.vars.example           ← μόνο για δοκιμή στον υπολογιστή
.gitignore
```

Η δομή που χρειάζεται το repo είναι η εξής: τα στατικά αρχεία (index.html, style.css, script.js, images/) μπαίνουν σε φάκελο `public/`. Αν είναι σε άλλο φάκελο, άλλαξε το `"directory"` στο `wrangler.jsonc`. Αν είναι στη ρίζα του repo, μετέφερέ τα σε `public/`. Διαφορετικά ο Worker θα σερβίρει και τα αρχεία κώδικα.

---

## Βήμα 1: Βάλε τα αρχεία στο repo

1. Αντέγραψε το `src/index.js` στο repo. Η φόρμα επικοινωνίας στέλνει μέσω Formspree, οπότε δεν χρειάζεται καμία ένωση κώδικα.
2. Βάλε τα `index.html`, `style.css`, `script.js` και τον φάκελο `admin/` μέσα στο `public/`, μαζί με τον φάκελο `images/` που έχεις ήδη. Τα `index.html` και `style.css` αντικαθιστούν τα παλιά.
3. Αντέγραψε τα `schema.sql`, `seed-demo.sql` και `.gitignore` στη ρίζα.

## Βήμα 2: Φτιάξε τη βάση D1

Από το dashboard (χωρίς τερματικό):

1. Cloudflare dashboard → **Storage & Databases** → **D1 SQL Database** → **Create**.
2. Όνομα: `coolfix-admin` → Create.
3. Αντέγραψε το **Database ID** που εμφανίζεται. Θα το χρειαστείς στο βήμα 4.
4. Άνοιξε τη βάση → καρτέλα **Console**. Επικόλλησε όλο το `schema.sql` → **Execute**.
   Αν βγάλει σφάλμα, τρέξε τις εντολές μία μία (κάθε `CREATE …;` χωριστά).

Εναλλακτικά από τερματικό:

```bash
npx wrangler d1 create coolfix-admin
npx wrangler d1 execute coolfix-admin --remote --file=schema.sql
```

## Βήμα 3 (προαιρετικό): Δοκιμαστικά δεδομένα για το demo

Για να δείχνει «ζωντανό» όταν το παρουσιάζεις στον πελάτη, τρέξε και το `seed-demo.sql` στο Console, ή με:

```bash
npx wrangler d1 execute coolfix-admin --remote --file=seed-demo.sql
```

Βάζει περίπου 140 ραντεβού στους τελευταίους 3 μήνες, μερικά για σήμερα και μερικά για τις επόμενες μέρες.
**Πριν ο πελάτης το χρησιμοποιήσει στ' αλήθεια**, σβήσ' τα από το Console με:

```sql
DELETE FROM jobs;
```

## Βήμα 4: Ρυθμίσεις Worker (wrangler.jsonc)

Άνοιξε το δικό σου `wrangler.jsonc` και πρόσθεσε τα `main`, `assets` και `d1_databases` όπως στο αρχείο του πακέτου. Βάλε το Database ID από το βήμα 2. Κράτα το δικό σου `"name"`.

Αν έχεις `wrangler.toml` αντί για jsonc, τα ίδια γράφονται έτσι:

```toml
name = "coolfix-demo"
main = "src/index.js"
compatibility_date = "2026-09-01"

[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = ["/api/*"]

[[d1_databases]]
binding = "DB"
database_name = "coolfix-admin"
database_id = "ΒΑΛΕ-ΕΔΩ-ΤΟ-DATABASE-ID"
```

Το binding βάλ' το στο αρχείο και όχι από το dashboard. Επειδή κάνεις deploy από GitHub, το αρχείο είναι αυτό που μετράει σε κάθε deploy.

## Βήμα 5: Κωδικός και κλειδί (secrets)

Dashboard → **Workers & Pages** → ο Worker σου → **Settings** → **Variables and Secrets** → **Add**. Διάλεξε τύπο **Secret** και για τα δύο:

| Όνομα            | Τιμή                                                                  |
|------------------|-----------------------------------------------------------------------|
| `ADMIN_PASSWORD` | ο κωδικός του ψυκτικού (τουλάχιστον 10 χαρακτήρες, όχι το όνομά του)  |
| `SESSION_SECRET` | ένα μακρύ τυχαίο κείμενο, 40+ χαρακτήρες                             |

Για το `SESSION_SECRET` μπορείς να πάρεις ένα έτοιμο τρέχοντας στο τερματικό `openssl rand -hex 32`. Διαφορετικά πληκτρολόγησε κάτι μακρύ και τυχαίο. Δεν χρειάζεται να το θυμάται κανείς.

Τα secrets δεν μπαίνουν ποτέ στο GitHub και μένουν ίδια σε κάθε deploy.

## Βήμα 6: Το κουμπάκι στην κεντρική σελίδα

Είναι ήδη μέσα στο `index.html`, λίγο πριν το `</body>`, και το στυλ του είναι στο τέλος του `style.css`.
Είναι ένα μικρό, σχεδόν διάφανο λουκέτο κάτω δεξιά που φαίνεται καθαρά όταν περάσει το ποντίκι από πάνω. Δεν χρειάζεται να κάνεις κάτι.

## Βήμα 7: Deploy και δοκιμή

1. Κάνε commit και push. Το Cloudflare κάνει deploy μόνο του.
2. Άνοιξε `https://coolfix-demo.runzarog.workers.dev/admin/` (ή πάτα το λουκέτο).
3. Βάλε τον κωδικό. Πρέπει να δεις τα σύνολα πάνω και τη φόρμα.

Αν κάτι δεν δουλεύει:

- **«Λείπουν τα secrets…»**: δεν έχει οριστεί το `ADMIN_PASSWORD` ή το `SESSION_SECRET` (βήμα 5).
- **«Σφάλμα διακομιστή»**: συνήθως δεν έχει τρέξει το `schema.sql` ή είναι λάθος το `database_id`. Δες τα logs στο dashboard → Worker → **Logs**.
- **Η σελίδα /admin/ βγάζει 404**: ο φάκελος `admin/` δεν είναι μέσα στο `public/`.

## Βήμα 8: Στο κινητό του ψυκτικού

Άνοιξε το `/admin/` στον browser του κινητού, κάνε login και πάτα **Προσθήκη στην αρχική οθόνη**. Στο iPhone είναι στο κουμπί κοινής χρήσης του Safari, στο Android στο μενού ⋮ του Chrome. Θα ανοίγει σαν εφαρμογή και μένει συνδεδεμένος 30 μέρες.

---

## Τι κάνει η εφαρμογή

- **Πάνω μπάρα**: εισπράξεις σήμερα, αυτή την εβδομάδα και αυτόν τον μήνα, μαζί με το πόσα ραντεβού είναι ακόμα κλεισμένα για σήμερα.
- **Νέο ραντεβού**: όνομα, τηλέφωνο, περιοχή, εργασία, συμφωνημένη τιμή, ποσό που εισπράχθηκε, κατάσταση και τρόπος πληρωμής.
  - Αν γράψει τηλέφωνο που υπάρχει ήδη, δείχνει «Γνωστός πελάτης» και συμπληρώνει μόνο του όνομα και περιοχή.
  - Όταν πατάει «Έγινε» και το ποσό είσπραξης είναι κενό, μπαίνει αυτόματα η συμφωνημένη τιμή.
- **Ραντεβού**: λίστα για σήμερα, εβδομάδα, μήνα ή όλα, με αναζήτηση που δουλεύει και χωρίς τόνους. Έχει κουμπί κλήσης δίπλα σε κάθε πελάτη και πάτημα για επεξεργασία ή διαγραφή. Υπάρχει και εξαγωγή σε CSV που ανοίγει σωστά στο ελληνικό Excel, π.χ. για τον λογιστή.
- **Γραφήματα**: εισπράξεις και αριθμός δουλειών ανά μέρα (30 μέρες), εβδομάδα (12 εβδομάδες) ή μήνα (12 μήνες). Επίσης κατανομή ανά τρόπο πληρωμής και οι περιοχές με τις περισσότερες εισπράξεις.

Στις εισπράξεις μετράνε μόνο τα ραντεβού με κατάσταση **Έγινε**.

## Ασφάλεια, με δυο λόγια

- Η σελίδα `/admin/` είναι απλό HTML και φαίνεται σε όποιον τη βρει, αλλά είναι άδεια. **Κανένα δεδομένο δεν φεύγει από τον server χωρίς σωστό κωδικό.**
- Μετά από 5 λάθος κωδικούς από την ίδια σύνδεση, το login κλειδώνει για 15 λεπτά.
- Η σύνδεση κρατιέται σε cookie που δεν διαβάζεται από JavaScript (HttpOnly) και δεν στέλνεται από άλλα sites (SameSite=Strict).
- **Αν χαθεί κινητό ή αλλάξεις κωδικό**, άλλαξε και το `SESSION_SECRET`. Έτσι αποσυνδέονται αμέσως όλες οι συσκευές.
- Για επιπλέον κλείδωμα αργότερα, μπορείς να βάλεις Cloudflare Access (δωρεάν) μπροστά από τα `/admin/*` και `/api/*`. Ο κώδικας δεν χρειάζεται αλλαγή.

## GDPR

Η εφαρμογή κρατάει ονόματα και τηλέφωνα πελατών. Για την πραγματική σελίδα του πελάτη χρειάζονται δύο πράγματα:

- Μια πρόταση στην πολιτική απορρήτου, π.χ. ότι τα στοιχεία επικοινωνίας κρατούνται για την οργάνωση των ραντεβού.
- Διαγραφή πελάτη όταν του ζητηθεί. Γίνεται από τη λίστα: πατάς το ραντεβού και μετά «Διαγραφή».

## Δοκιμή στον υπολογιστή (προαιρετικό)

```bash
cp .dev.vars.example .dev.vars
npx wrangler d1 execute coolfix-admin --local --file=schema.sql
npx wrangler d1 execute coolfix-admin --local --file=seed-demo.sql
npx wrangler dev
```

Μετά άνοιξε `http://localhost:8787/admin/` με κωδικό `dokimi123`.
